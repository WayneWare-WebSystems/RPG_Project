<header>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</header>
<style>
/* IMPORTAR FONTES PARA TODO PROJETO */
@@import url('https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap');
@@import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100..900;1,100..900&display=swap');
/* VARIÁVEIS CSS GLOBAIS */
:root {
    --tr: transparent;
    --hex-1: #1a1a1a;
    --hex-2: #fff;
    --hex-3: #000;
    --hex-4: #2d2d2d;
    --hex-5: #212020;
    --hex-6: #030202;
    --hex-7: #2c2c2c;
    --hex-8: #3aa856;
    --hex-9: #34974d;
    --hex-10: #2e8644;
    --hex-11: #b8a269;
    --hex-12: #1E1E1E;
    --hex-13: #5e5e5e;
    --hex-14: #d10000;
    --hex-15: #900000;
    --hex-16: #0000d1;
    --hex-17: #000090;
    --rgba-1: rgba(0,0,0,0.5);
    --rgb-1: rgba(118,118,118);
}

/* ESTILIZAÇÃO ELEMENTOS GLOBAIS*/

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

a {
    text-decoration: none;
}

button, textarea, a, input {
    outline: none;
}

html, body {
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100vh;
    margin: 0;
    padding: 0;
    font-family: "Open Sans", serif;
    font-optical-sizing: auto;
    font-weight: 400;
    line-height: normal;
    vertical-align: middle;
    letter-spacing: normal;
    font-style: normal;
    font-size: 18px;
    -webkit-font-smoothing: antialiased;
    color: var(--hex-2);
    background-color: var(--hex-1);
}

header {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: auto;
    background-color: var(--hex-3);
}

header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-direction: row;
    width: 100%;
    height: auto;
    margin: 0 30px;
    padding: 20px 0;
    max-width: 1200px;
}

header .container .titulo {
    font-family: "Montserrat", sans-serif;
    font-weight: 500;
    line-height: 50px;
    vertical-align: middle;
    letter-spacing: 5px;
    font-style: normal;
    font-size: 35px;
    color: var(--hex-11);
}

header .container .container-usuario {
    display: flex;
    align-items: center;
    flex-direction: row;
    width: auto;
    height: auto;
    border-radius: 25px;
    background-color: var(--hex-4);
}

header .container .container-usuario .box-usuario {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    overflow: hidden;
    background-color: var(--hex-4);
    border: 3px solid var(--hex-5);
    cursor: pointer;
}

header .container .container-usuario button {
    display: none;
    font-family: "Montserrat", sans-serif;
    font-weight: 500;
    line-height: normal;
    vertical-align: middle;
    letter-spacing: 3px;
    font-style: normal;
    font-size: 14px;
    color: var(--hex-2);
    background-color: var(--tr);
    border: none;
    margin: 0 10px;
    cursor: pointer;
}

header .container .container-usuario button:active {
    display: none;
}

header .container .container-usuario:hover button,
header .container .container-usuario .box-usuario:hover + button {
    display: flex;
}

main {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    flex-direction: column;
    width: 100%;
    height: 100%;
}

footer {
}
</style>